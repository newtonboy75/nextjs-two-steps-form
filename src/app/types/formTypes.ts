export type Step1FormValues = {
  email: string;
  firstName: string;
  lastName: string;
  interest: string;
};

export type Step2FormValues = {
  termsAccepted: boolean;
  favoriteInterest: string;
};
